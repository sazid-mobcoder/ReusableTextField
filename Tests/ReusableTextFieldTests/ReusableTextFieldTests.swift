import XCTest
@testable import ReusableTextField

final class ReusableTextFieldTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ReusableTextField().text, "Hello, World!")
    }
}
